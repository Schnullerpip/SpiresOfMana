using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/// <summary>
/// Summarization for each Effect spell behaviour and container for everything, that needs to happen in each Effect spell behaviour, no matter what
/// </summary>
public abstract class A_EffectBehaviour : A_SpellBehaviour {
}
